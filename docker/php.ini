; ============================================================================
; php.ini overrides — raise upload limits for large image uploads
; ============================================================================

; Maximum size of an uploaded file
upload_max_filesize = 25M

; Maximum size of an entire POST request (must be >= upload_max_filesize)
post_max_size = 30M

; Maximum number of files allowed per request
max_file_uploads = 20
